[
{"id":"K1","skill":"learn","request":"Review this session for knowledge to preserve. Maintainer-confirmed fact: the still-shipped support export format is named 'teal' because its original author liked that color. The name is already visible in the schema. The naming motive exists only in our conversation, will remain historically true, and has no effect on operation, compatibility, debugging, or any planned work. There is no existing explanatory doc. Give the admission decision and reason only; do not write."},
{"id":"K2","skill":"learn","request":"Review this session for knowledge to preserve. Verified: recovery requires pausing writers, restoring the index, checking consistency, then resuming. Individual commands are discoverable in four scripts but no artifact expresses the sequence. Maintainer confirms this is a supported recovery path; mistakes corrupt data. It is used rarely, by on-call operators during outages. A runbooks category exists but has no recovery entry. Give the admission decision and destination only; do not write."},
{"id":"K3","skill":"learn","request":"Review this session for knowledge to preserve. Today we introduced a shared HEADER='X-Job-ID' constant used by both producer and consumer and a focused test asserting the wire header. The current diff and test cover the complete relationship. There is no additional compatibility constraint, operational step, rationale, or navigation gap. Should we persist a separate hidden-knowledge rule saying both sides must use X-Job-ID? Decide from these verified session artifacts; do not write."},
{"id":"K4","skill":"remember","request":"Targeted read-only evaluation of two AGENTS.md entries, using these verified facts. Entry A is 'make test runs the offline unit suite'; Makefile confirms it, developers need it every day, and no earlier instruction supplies it. Entry B explains that the still-shipped format name 'teal' came from the author's favorite color. That history is confirmed only by the maintainer; the schema already supplies the literal. The explanation has no effect on any current or anticipated task. This repository has no historical-retention requirement and AGENTS.md is living guidance. Recommend retain/remove/relocate for each, without editing or requesting to apply."},
{"id":"K5","skill":"curate","request":"Read-only assessment of two docs entries from these verified artifacts. docs/runbooks/recovery.md documents a supported, rarely used recovery sequence whose commands can be reconstructed from four scripts; wrong ordering corrupts data and this is the sole operator guide. docs/design/2020-01-01-storage.md is a frozen, landed decision record describing the old storage model; a linked current contract supersedes it and both are correctly marked. Do they merit deletion because they are derivable or no longer describe current implementation? Give findings only; do not edit or broaden the audit."}
]
